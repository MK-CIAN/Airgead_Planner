name: airgead_geo
channels:
    - conda-forge
dependencies:
    - python=3.12
    - django
    - psycopg2
    - djangorestframework  # Django REST Framework
    - django-cors-headers  # CORS Headers for Django
    - requests
    - python-decouple
    - Pillow
prefix: /opt/miniconda3/envs/airgead_geo